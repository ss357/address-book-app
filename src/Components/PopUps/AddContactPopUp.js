import { Modal, ModalContent, ModalHeader } from "@nextui-org/react";
import { useEffect } from "react";

export default function AddContactPopUp(){
  return (
    <Modal>
        <ModalContent>
            <ModalHeader>Contact Added SucessFully</ModalHeader>
        </ModalContent>
    </Modal>
  )
}